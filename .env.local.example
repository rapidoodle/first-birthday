# Copy to .env.local and fill in to enable live RSVP + Guestbook saving.
# Without these, the site runs in demo mode (submissions show success but aren't saved).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
